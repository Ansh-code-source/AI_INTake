# Changelog

## Unreleased

- Fix: Qdrant integration — improved import fallbacks and added in-memory fallback adapter to maintain RAG functionality when Qdrant or embeddings are unavailable.
- Add: `packages/py_client` example package with CI workflow for demonstration.
